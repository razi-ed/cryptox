
/**
 *Reducer for trade-exchage
 * @param {*} state
 * @param {*} action
 * @return {reducers}
 */
export default function Exchage(state={}, action) {
    switch (action.type) {
        default:
        return state;
    }
};
